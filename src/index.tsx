import 'normalize.css';
import ReactDOM from 'react-dom/client';
import App from './App';
import f from './components/UserDictionaryAPI/ffff';

console.log(f());
const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(<App />);
