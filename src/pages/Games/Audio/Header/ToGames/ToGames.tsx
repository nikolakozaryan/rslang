import React from 'react';
import classes from './ToGames.module.scss';

const ToGames = () => (
  <div className={classes.cross__container}>
    <div className={classes.cross}></div>
  </div>
);

export default ToGames;
